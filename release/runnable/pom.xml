<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
    <parent>
  		<groupId>nl.utwente.groove</groupId>
	    <artifactId>release</artifactId>
	    <version>1.0</version>
    </parent>
	<artifactId>runnable</artifactId>
	<packaging>pom</packaging>

	<modules>
		<module>Simulator</module>
		<module>Generator</module>
		<module>Imager</module>
		<module>Viewer</module>
		<module>ModelChecker</module>
	</modules>
	
	<properties>
		<runnable.basedir>${project.parent.basedir}</runnable.basedir>
		<release.basedir>${project.parent.parent.basedir}</release.basedir>
	</properties>
	
	<build>
		<finalName>${project.artifactId}</finalName>

		<resources>
			<resource>
				<directory>..</directory>
				<includes>
					<include>README.txt</include>
				</includes>
			</resource>
		</resources>

		<plugins>
			<plugin>
				<artifactId>maven-jar-plugin</artifactId>
			    <configuration>
			   		<outputDirectory>${runnable.basedir}/target</outputDirectory>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<classpathPrefix>../lib/</classpathPrefix>
							<mainClass>${project.groupId}.${project.artifactId}</mainClass>
						</manifest>
						<manifestEntries>
							<!-- FlatLaf loads a native library for the window
							     decorations, which JDK 24+ (JEP 472) reports with
							     a warning unless native access has been granted.
							     The java launcher honours this attribute in the
							     main jar of a java -jar start (and so in the
							     jpackage launchers, which start the same way),
							     for all code on the class path; older JDKs
							     ignore it. -->
							<Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
						</manifestEntries>
					</archive>
			    </configuration>  
			</plugin>
		</plugins>
	</build>
</project>
