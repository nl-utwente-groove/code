# Normalize all text files to LF in the repository; working-tree endings
# are left to each checkout's own configuration (e.g. core.autocrlf).
* text=auto

# Shell scripts must keep LF line endings, also on Windows checkouts
# (the GitHub windows runners check out with autocrlf, and bash rejects CRLF)
*.sh text eol=lf
