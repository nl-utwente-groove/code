<project xmlns="https://maven.apache.org/POM/4.0.0" xmlns:xsi="https://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>nl.utwente.groove</groupId>
	<artifactId>groove-gxl</artifactId>
	<version>3.0.1</version>
	<packaging>jar</packaging>

	<name>GROOVE GXL bindings</name>
	<description>
	JAXB bindings for the GXL graph exchange language (http://www.gupro.de/GXL/),
	generated and maintained by the GROOVE project
	(https://nl-utwente-groove.github.io). Unlike its sibling repackaged
	artifacts, this is first-party GROOVE code; the jar carries a proper
	module-info (module nl.utwente.groove.gxl). Identical to the 3.0 jar
	previously bundled inside the GROOVE distribution.
	</description>
	<url>https://nl-utwente-groove.github.io</url>

	<licenses>
		<license>
			<name>The Apache License, Version 2.0</name>
			<url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
		</license>
	</licenses>

	<developers>
		<developer>
			<name>Arend Rensink</name>
			<email>arend.rensink@utwente.nl</email>
			<organization>University of Twente, The Netherlands</organization>
			<organizationUrl>http://people.utwente.nl/arend.rensink</organizationUrl>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:https://github.com/nl-utwente-groove/code.git</connection>
		<developerConnection>scm:git:https://github.com/nl-utwente-groove/code.git</developerConnection>
		<url>https://github.com/nl-utwente-groove/code.git</url>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<upstream.version>3.0</upstream.version>
		<upstream.dir>${project.basedir}/../upstream</upstream.dir>
	</properties>

	<dependencies>
		<dependency>
			<groupId>jakarta.xml.bind</groupId>
			<artifactId>jakarta.xml.bind-api</artifactId>
			<version>3.0.0</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>3.1.0</version>
				<executions>
					<execution>
						<!-- unpacks the existing binary jar (incl. its
						     module-info.class); the manifest is regenerated by
						     maven-jar-plugin below -->
						<id>unpack-upstream</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<unzip src="${upstream.dir}/${project.artifactId}-${upstream.version}.jar" dest="${project.build.outputDirectory}">
									<patternset>
										<exclude name="META-INF/MANIFEST.MF"/>
									</patternset>
								</unzip>
							</target>
						</configuration>
					</execution>
					<execution>
						<!-- Maven Central requires a javadoc jar to be present;
						     the bindings are generated code without javadoc,
						     so a stub explaining that is packaged instead -->
						<id>javadoc-stub</id>
						<phase>package</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<jar destfile="${project.build.directory}/${project.build.finalName}-javadoc.jar" basedir="${project.basedir}/src/javadoc"/>
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.1</version>
				<configuration>
					<archive>
						<manifestEntries>
							<!-- no Automatic-Module-Name: the jar contains a
							     module-info.class declaring module
							     nl.utwente.groove.gxl -->
							<Specification-Title>GROOVE GXL bindings</Specification-Title>
							<Specification-Version>${upstream.version}</Specification-Version>
							<Implementation-Title>GROOVE GXL bindings</Implementation-Title>
							<Implementation-Version>${upstream.version}</Implementation-Version>
							<Implementation-Vendor>University of Twente, The Netherlands</Implementation-Vendor>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>attach-sources-and-javadoc</id>
						<phase>package</phase>
						<goals>
							<goal>attach-artifact</goal>
						</goals>
						<configuration>
							<artifacts>
								<artifact>
									<file>${upstream.dir}/${project.artifactId}-${upstream.version}-sources.jar</file>
									<type>jar</type>
									<classifier>sources</classifier>
								</artifact>
								<artifact>
									<file>${project.build.directory}/${project.build.finalName}-javadoc.jar</file>
									<type>jar</type>
									<classifier>javadoc</classifier>
								</artifact>
							</artifacts>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>maven</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>0.9.0</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>central</publishingServerId>
							<!-- deliberately not auto-published: validate the
							     bundle in the Central portal before releasing;
							     Central publications are irrevocable -->
							<autoPublish>false</autoPublish>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.1.0</version>
						<executions>
							<execution>
								<id>verify-sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
						<configuration>
							<gpgArguments>
								<arg>--pinentry-mode</arg>
								<arg>loopback</arg>
							</gpgArguments>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
